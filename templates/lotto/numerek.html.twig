{% extends 'base.html.twig' %}

{% block body %}

    {% if number==13 %}

        {% for i in 1..number %}

            <h1>Twoj nieszczęśliwy numer {{ number }}</h1>

        {% endfor %}

    {% else %}
        <h1>Twoj numer {{ number }}</h1>
    {% endif %}

{% endblock %}